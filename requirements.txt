# DISPATCH — pinned at hackathon. Core deps are REQUIRED; optional deps degrade gracefully.

# --- core (required; app runs on these alone) ---
streamlit>=1.33      # UI; bundles altair (used for the scaling chart)
networkx>=3.0
numpy>=1.24
pandas>=2.0
requests>=2.28
pytest>=7.0
# the draggable map vendors MapLibre GL locally (components/dispatch_map/vendor) — no pip dep
# .env is read by a tiny built-in loader in app.py — no python-dotenv needed

# --- optional (each has a pure-Python fallback; install best-effort) ---
ortools>=9.8        # classical VRP/assignment; falls back to greedy solver
anthropic>=0.25     # agent NL parse + narration; falls back to deterministic
qiskit>=1.0         # quantum QAOA; falls back to numpy simulated-annealer
qiskit-aer>=0.14
qiskit-optimization>=0.6
