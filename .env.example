# DISPATCH environment — copy to .env and fill in. .env is gitignored.
# Live Claude crisis-parsing + dispatcher narration (deterministic fallback if unset):
ANTHROPIC_API_KEY=
# Live XpyQ /decisions backend (annealer stand-in if unset):
XPYQ_API_KEY=
# XPYQ_ENDPOINT=https://xpyq.vercel.app/api/decisions
